# Frontend

La aplicación React de El Túnel contiene el flujo público de reservas y el
panel de administración. La documentación general, configuración y comandos
están en el [README raíz](../README.md).

Comandos disponibles:

```powershell
npm ci
npm run dev
npm run lint
npm run build
```
